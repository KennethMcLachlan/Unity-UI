using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class CannonBallBehavior : MonoBehaviour
{
    

    ////private Rigidbody _rigidbody;

    

    //[SerializeField]
    //private float _thrust = 5f;

    ////private bool _cannonHasFired;

    ////private void FixedUpdate()
    ////{
    ////}
    ////public void FireCannon()
    ////{
    ////    GameObject cannon = Instantiate(_cannonBall, _ballSpawnPoint.transform.position, Quaternion.identity);
    ////    _cannonHasFired = true;
    ////    cannon.GetComponent<Rigidbody>().AddRelativeForce(Vector3.forward * _thrust * Time.deltaTime);
    ////    _cannonHasFired = false;
    ////}

    //private void Update()
    //{
    //    //if (Input.GetKeyDown(KeyCode.Space))
    //    //{
    //    //    GameObject cannon = Instantiate(_cannonBall, _ballSpawnPoint.transform.position, Quaternion.identity);
    //    //    cannon.GetComponent<Rigidbody>().AddRelativeForce(new Vector3(-500, 0, 0));
    //    //    //cannon.GetComponent<Rigidbody>().AddRelativeForce(Vector3.forward * _thrust * Time.deltaTime);
    //    //}

    //    FireCannon();

    //}

    //public void FireCannon()
    //{

    //    GameObject cannon = Instantiate(_cannonBall, _ballSpawnPoint.transform.position, Quaternion.identity);
    //    cannon.GetComponent<Rigidbody>().AddRelativeForce(new Vector3(-500, 0, 0));

    //}

}
